import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Form extends Component {
    render() {
        return (
           <form>
               <h1>Form</h1>
           </form>
        );
    }
}

Form.propTypes = {

};

export default Form;