import React from 'react';
import PropTypes from 'prop-types';

const Heading = props => {
    return (
        <div>
            <h1>Resurants Header</h1>
        </div>
    );
};

Heading.propTypes = {
    
};

export default Heading;
